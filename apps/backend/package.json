{
  "name": "backend",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "//exports": "Les types générés, pas un build : `apps/web` importe `api` et `Id` en `import type` ou via le client Convex, donc seule la forme du déploiement traverse la frontière — une fonction retirée casse le client à la compilation.",
  "exports": {
    ".": {
      "types": "./convex/_generated/api.d.ts",
      "default": "./convex/_generated/api.js"
    },
    "./entitlements": {
      "types": "./convex/entitlements.ts",
      "default": "./convex/entitlements.ts"
    },
    "./media": {
      "types": "./convex/media.ts",
      "default": "./convex/media.ts"
    }
  },
  "scripts": {
    "//dev": "Anonyme par défaut : un backend réel sur la machine, sans Docker et sans compte Convex, là où `convex dev` seul ouvre un navigateur pour se connecter. Le mode force le déploiement `anonymous-agent`, donc il ne peut pas pousser par erreur sur le déploiement de développement partagé — c'est `dev:cloud` qui sert à cela, explicitement.",
    "dev": "CONVEX_AGENT_MODE=anonymous convex dev",
    "dev:once": "CONVEX_AGENT_MODE=anonymous convex dev --once",
    "dev:cloud": "convex dev",
    "//deploy": "Chaque cible est une clé de déploiement, pas un nom : `CONVEX_DEPLOY_KEY` borne la commande à un déploiement, là où `CONVEX_DEPLOYMENT` ne désigne qu'un projet — dont `deploy` prend alors la production, quel que soit le déploiement nommé. Les deux fichiers ne portent que cette clé, restent hors du dépôt, et se recréent par `convex deployment token create … --save-env` ; `environnements.md` donne la commande.",
    "deploy:preprod": "convex deploy --env-file .env.preprod",
    "deploy:prod": "convex deploy --env-file .env.production",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test:unit": "AUTH_TEST_PASSWORD=1 SITE_URL=http://localhost:5173 vitest run convex"
  },
  "dependencies": {
    "@auth/core": "0.41.3",
    "@convex-dev/auth": "^0.0.94",
    "@convex-dev/rate-limiter": "^0.3.2",
    "@polar-sh/sdk": "^0.35.0",
    "convex": "^1.43.0"
  },
  "devDependencies": {
    "@edge-runtime/vm": "^5.0.0",
    "@types/node": "^26.2.0",
    "convex-test": "^0.0.55",
    "jose": "^6.2.8",
    "standardwebhooks": "^1.0.0",
    "typescript": "^7.0.2",
    "vite": "^8.2.1",
    "vitest": "^4.1.10"
  }
}
